# HandyG Frontend

Development:

1. cd handyg-frontend
2. npm install
3. npm run dev
4. Open http://localhost:5173

To connect to a remote backend during dev, create a file `.env` in the frontend folder with:

VITE_API_URL=https://your-backend.example.com
